# HealthCloud — local development stack
#
# PLACEHOLDER (Phase 0). The real services are added in Phase 1:
#   - postgres   (PostgreSQL 17 — app database + Spring Session tables)
#   - backend    (Spring Boot 4.1 BFF/API)
#   - frontend   (React/Vite dev server)
# Later phases add: kafka (KRaft), mailpit (email testing), a fake malware scanner.
#
# Kept intentionally empty until Phase 1 so `docker compose up` isn't run before the app exists.
services: {}
