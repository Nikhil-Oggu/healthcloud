# Copy this file to `.env` for local development and adjust as needed.
# `.env` is git-ignored and must never contain real secrets.
# Docker Compose reads these automatically; the Spring app falls back to the same
# defaults (healthcloud/healthcloud) if the variables are not set, so `.env` is optional locally.

HEALTHCLOUD_DB_USER=healthcloud
HEALTHCLOUD_DB_PASSWORD=healthcloud
